% Generated by roxygen2 (4.0.1): do not edit by hand
\name{arbitraryOrderCorrelation}
\alias{arbitraryOrderCorrelation}
\title{Partial correlations}
\usage{
arbitraryOrderCorrelation(S, z)
}
\arguments{
\item{X}{A numeric matrix with observations in rows and variables
in columns.}

\item{S}{A numeric correlation matrix}

\item{z}{A integer vector of indicies of the variabels to condition on. I.e.
length(z) is the order of the partial correlations. If \code{z} has
length 0 the marginal correlations are returned.}
}
\value{
A numeric matrix of the same size as the correlation matrix
  with the partial correlations given the variabels indexed by \code{z}.
  \code{NaN} are returned in the rows and columns of the conditioned
  variables.
}
\description{
Compute arbitrary \code{k}th order partial correlations using recursion.
This implementation is very inefficient.
}
\examples{
X <- createData(6, 20)
Y <- createData(6, 20)
Z <- createData(6, 20)
S <- cor(X)
arbitraryOrderCorrelation(S, z = c(2, 3, 5))
}
\seealso{
\link{\code{correlation}} \link{\code{correlation}}
}

